<p style="color: green"><%= notice %></p>

<h1>Products</h1>

<div id="products">
  <% @products.each do |product| %>
    <p>
      <%= product.name %> | <%= product.price %> | <%= link_to "Show this product", product %> | comprar
    </p>
  <% end %>
</div>

<%= link_to "New product", new_product_path %>

<%= link_to "Volver", root_path %>

